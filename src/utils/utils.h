#pragma once
#include <map>
#include <string>

namespace NSTTF
{
    namespace utils
    {
        std::map<const std::string, const AbstractOperation *> operationNameMap = {
            // TODO: Add more operations
        };
    }
}
